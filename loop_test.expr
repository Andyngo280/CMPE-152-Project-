program Test;
const
	great = 30;
	quantity = 20;
type
	arr = array [1..20] of array [2..30] of integer;
	time = boolean;
var
	alpha, beta, gamma, omega : integer;
	phi : arr;
	over: boolean;

begin
	beta := 1;
	alpha := 3;

	REPEAT
		alpha := alpha - 1;
		beta := beta * 2;
	UNTIL alpha = 0;

	while(beta > 6) do 
		beta := beta - 1;

	for omega := 1 to 5 do
		beta := beta + 1;
end.